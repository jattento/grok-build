# Report format

`report.py` must print exactly two lines to stdout and nothing else:

```
total=<total value, two decimals>
low=<low-stock SKUs, comma separated, no spaces>
```

Both values come from `stock.py`. Use the default threshold for `low`.

`total.txt` must contain the total value on its own, two decimals, nothing else.
